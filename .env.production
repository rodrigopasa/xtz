# Configuração da aplicação para ambiente de produção
NODE_ENV=production
PORT=5000
HOST=0.0.0.0
SESSION_SECRET=sua_chave_secreta_longa_aqui
COOKIE_SECURE=true

# IMPORTANTE: Esta variável indica que devemos usar os arquivos estáticos compilados
# em vez do servidor de desenvolvimento do Vite
VITE_DEV_SERVER_ENABLED=false

# Configuração do banco de dados PostgreSQL (Neon)
DATABASE_URL=postgresql://usuario:senha@seu-host-neon.com/banco_de_dados?sslmode=require

# Se necessário, variáveis individuais do PostgreSQL (usadas por alguns provedores)
# PGHOST=seu-host-neon.com
# PGUSER=usuario
# PGPASSWORD=senha_segura
# PGDATABASE=banco_de_dados
# PGPORT=5432

# Configurações de upload para ambiente de produção
UPLOAD_DIR=/app/uploads
MAX_FILE_SIZE=50000000

# Configurações de diretórios de armazenamento para produção
BOOKS_DIR=/app/public/books
COVERS_DIR=/app/public/covers